<div class="container">
    <div class="row">
        <%= month_calendar events: @posts do |date, posts| %>
            <%= date.day %>  
                <% posts.each do |post| %>
                    <%= link_to post.title, post_path(post) %>
                <% end %>
        <% end %>
    </div>
</div>